# Build/scan output from local workflow runs — never committed.
packages/
*.nupkg

# Real secrets for a local/interactive update_all.ps1 / test_all.ps1 run —
# see internal/README.md. update_vars_default.ps1 (the template) IS committed.
update_vars.ps1
