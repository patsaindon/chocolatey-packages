# Template — copy this folder to internal/<package-id>/ and fill in.
packageId: pathcopycopy
owner: IT Operations                  # owning team, drives notification routing
notes: "Surfaced by version-mismatch triage of prospecting/community-version-reference.csv. Community's matched package was stuck at 3.12.3; vendor's real current release is v20.0 (now requires Windows 7 SP1 and .NET Framework 4.8). Tracks vendor's GitHub releases directly."
