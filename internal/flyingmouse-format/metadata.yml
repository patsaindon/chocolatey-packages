# Template — copy this folder to internal/<package-id>/ and fill in.
packageId: flyingmouse-format
owner: IT Operations                  # owning team, drives notification routing
notes: "Surfaced by prospect-packaging-opportunities.yml's automated discovery (real GitHub activity — 4,198 stars, created 2026-08-06, no existing Chocolatey Community package found via choco search flyingmouse-format --exact). Ships real Windows installer assets on its GitHub Releases page: FlyingMouse-Format-Setup-0.6.4-x64.exe and FlyingMouse.Format-Setup-0.6.4-win7-x64.exe. No confirmed silent-install switch found via winget, Community package lookup, or ManageEngine catalog search."
