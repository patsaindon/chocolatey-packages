# Template — copy this folder to internal/<package-id>/ and fill in.
packageId: rufus
owner: IT Operations                  # owning team, drives notification routing
notes: "Real end-to-end test using evergreen-api-tracked software, per the user's request to validate the pipeline with a few evergreen apps. Rufus is evergreen-tracked as \"AkeoRufus\". It publishes no x64 variant at all (only ARM64 and several x86 builds). Rufus itself is actually a single portable executable with no real installer/silent-install-switch concept in practice, despite evergreen's own data labeling its variants \"Default\" rather than \"Portable\" -- packaged here as portable (Get-ChocolateyWebFile), per human review on PR #24."
