# Template — copy this folder to internal/<package-id>/ and fill in.
packageId: codegraphcontext
owner: IT Operations                  # owning team, drives notification routing
notes: "An MCP server + CLI tool that indexes local code into a graph database for AI assistants (MIT licensed). Surfaced by prospect-packaging-opportunities.yml automated discovery (4,111 stars, no existing Chocolatey Community package found via choco search --exact)."
